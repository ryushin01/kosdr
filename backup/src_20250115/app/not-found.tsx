"use client";

// import { useRouter } from "next/navigation";

export default function NotFound() {
  // const router = useRouter();

  // const toPreviousPage = () => router.back();
  // const toMainPage = () => router.push(MAIN_PATH);

  return (
    <div>
      {/*이전 페이지 돌아가기*/}
      {/*메인 페이지(홈) 돌아가기*/}
    </div>
  );
}
